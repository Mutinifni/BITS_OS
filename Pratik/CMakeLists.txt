# assuming that OS_DEV_SRC and OS_DEV_BUILD are set. 
cmake_minimum_required(VERSION 2.6)
PROJECT (myos)
enable_language(ASM-NASM)
set(CMAKE_VERBOSE_MAKEFILE on)
set(CMAKE_BINARY_DIR $ENV(OS_DEV_INCLUDE_PATHS))
#set(OS_DEV_INCLUDE_PATH $ENV(OS_DEV_SRC)/include/kernel)
#set(INCLUDE_OS_DIRECTORIES $ENV(OS_DEV_INCLUDE_PATHS))  	
set(SOURCE_PATH $ENV(OS_DEV_SRC))
set(BUILD_PATH $ENV(OS_DEV_BUILD)) 
set(CMAKE_C_FLAGS "-std=c99 -m32 -c -nostdinc")
